apiVersion: v1
kind: Pod
metadata:
  name: expense1
  labels:
    project: expense
spec:
  containers:
  - name: nginx
    image: nginx  
  tolerations:
  - key: "hardware"
    operator: "Equal"
    value: "gpu"
    effect: "NoSchedule"